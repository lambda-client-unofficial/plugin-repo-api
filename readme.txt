API for the plugin repo, probably used for integration with other things.

Look at src/routes/Home.ts for usage.

To run: give deno net and read permissions.
    deno run --allow-net --allow-read src/main.ts
